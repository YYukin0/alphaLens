# Copy to .env in project root for docker compose
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Railway production (set in Railway dashboard, not here)
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# REDIS_URL=${{Redis.REDIS_URL}}
# SEC_USER_AGENT=AlphaLens contact@your-email.com
# CORS_ORIGINS=https://your-frontend.up.railway.app
# VITE_API_BASE_URL=https://your-backend.up.railway.app/api/v1
